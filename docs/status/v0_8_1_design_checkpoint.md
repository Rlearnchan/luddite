# v0.8.1 Design Checkpoint

## New confirmations

- `jibi` may directly append to Google Sheet.
- Bot rows must be visually distinguishable from human rows.
- Collection should run every day, not only Monday/Tuesday.
- Digest can be weekday morning by default.
- Slack should be planned as a separate Luddite bot.
- `syuka-ops` remains a future integration source for past video similarity/performance.
- First real demo target remains daily candidate digest, not PPT automation.

## Next Codex instruction

Apply these docs after the v0.7 eval harness checkpoint, then proceed to v0.8 manual dry run and v0.9 jibi Daily Digest MVP planning.
